export enum DateFormat {
  DOT_FORMAT = 'DD.MM.YYYY',
  SLASH_FORMAT = 'DD/MM/YYYY'
}

export type Nullable<T> = T | null

export enum VariantPrefix {
  Title = "title",
  Item = "item",
}

export enum Variant {
  Primary = 'Primary',
  Secondary = 'Secondary',
}